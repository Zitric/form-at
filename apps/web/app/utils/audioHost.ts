// Transitional re-export — the canonical source moved to
// packages/data/src/sets.ts (alongside the sets catalogue that builds these
// URLs) so apps/admin can read the same constants. See TECH_DEBT.md item 21
// for the proposed follow-up sweep of apps/web's remaining `~/data/sets`
// import sites to `@form-at/data` directly.
export { AUDIO_HOST, AUDIO_ORIGIN } from "@form-at/data/sets";
